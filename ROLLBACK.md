# 복구 / 배포 전 확인

## 체크포인트

- 기준: `c409ce16b577351b389c1ba20fcb7bbefe284555`
- 개선 작업 브랜치: `codex/review-safety`
- 개선 작업 폴더: `/Users/kunmoo/건무개발/unpa-worklog`
- 기준 버전의 독립 체크아웃: `/Users/kunmoo/건무개발/unpa-worklog-baseline` (변경 없음)
- 전체 Git 이력 백업: `/Users/kunmoo/건무개발/unpa-worklog-rollback-c409ce1.bundle` (`git bundle verify` 통과)
- 작업 중 기존 임시 체크아웃 일부가 정리된 것을 발견하여, 현재 폴더는 임시 경로에 의존하지 않는 독립 Git 저장소로 전환했습니다. 이전 worktree 연결 파일은 `/Users/kunmoo/건무개발/unpa-worklog-worktree-link.backup`에 보관했습니다.
- 로컬 검증 후 사용자의 별도 요청으로 커밋·푸시를 진행합니다. 운영 CMS 전송은 하지 않습니다.

## 안전한 이전 코드 확인

기준 버전은 이미 `unpa-worklog-baseline` 폴더에 복원되어 있습니다. 필요하면 bundle에서 또 다른 비어 있는 경로로 복원할 수 있습니다:

```sh
git clone --branch codex/review-safety /Users/kunmoo/건무개발/unpa-worklog-rollback-c409ce1.bundle /Users/kunmoo/건무개발/unpa-worklog-restored
```

브라우저 기록과 CMS 전송 결과는 Git으로 되돌릴 수 없습니다. 실제 사용 전에 업무일지/검수 JSON을 각각 내보내세요. 브라우저 저장소는 지우지 마세요. 실행 기록은 CMS 출처의 `unpa-console-journal-v2`, 스캔/선택은 `unpa-console-session-v2`에 남습니다. 구버전으로 돌아가더라도 이 기록을 먼저 백업해야 합니다.

## 배포 전 운영 읽기 검증

1. `npm test` / `npm run build`를 실행하고 변경 내역을 검토합니다.
2. CMS의 실제 브랜드/제품/옵션 응답 스키마를 GET으로 확인합니다. 승인 필터를 생략한 전체 목록이 지원되는지, 페이지 크기·전체 수가 올바른지 확인합니다. 지원되지 않으면 보류로 남겨야 합니다.
3. 정샘물 엔라이트, 록시땅 릴랙싱, 다이소 라임민트 사례를 읽기 스캔으로 대조합니다. 유사 후보는 확정이 아니라 제안이어야 합니다.
4. 제품 이미지 실패, 첨부 없음, 정상 짧은 본문, 콤부차를 각각 확인합니다. 그리드는 기본 미선택이어야 합니다.
5. 실행은 별도 승인 후 **1건**부터 진행합니다. 재조회 변화 시 중단, 응답 유실 시 재실행 차단, CMS 결과와 로그 일치를 확인합니다.
6. 업무일지에 같은 결과를 두 번 반영해 두 번째 증가가 0인지 확인합니다. 서로 다른 두 실행의 검수 기록이 같은 날짜에 함께 남는지도 확인합니다.
7. Supabase는 계정별 로컬 분리와 조건부 업데이트를 사용합니다. 실제 RLS/insert/update 권한, 기존 기록 이전, 두 기기 충돌을 별도로 검증하세요. 충돌 시 자동 덮어쓰기는 하지 않으며 두 데이터를 백업해 비교해야 합니다.

## 알려진 경계

- 이미지 의미/발색샷 자동 판독 없음: 사람의 확인이 필요합니다.
- CMS API는 문서화되지 않은 외부 의존성입니다. 로컬 모의 테스트가 운영 검증을 대체하지 않습니다.
- 통신 실패의 서버 적용 여부를 단정하지 않습니다. 자동 재전송하지 않고 CMS에서 직접 대조합니다.
- Web Locks 지원 브라우저에서는 같은 출처의 탭 간 실행을 잠급니다. 다른 브라우저/기기의 실행 잠금은 공유되지 않습니다. 동시에 두 콘솔을 실행하지 마세요.
- 검수 상세는 JSON/브라우저 저장, 월별 정산만 Supabase 동기화입니다.
