# 개선 및 검증 기록 — 2026-09-10

추가 반영: 현재 사용자 정지(`userBlocked=true`)는 최우선 미노출 분류, 해제(`false`)는 일반 검수 유지. 횟수는 참고정보이며 처벌 기준으로 쓰지 않습니다. 목록/상세 상태 불일치·누락은 보류하고 실행 전에도 재확인합니다. 정지 상태 및 횟수는 콘솔 목록과 JSON에 포함합니다. 추가 테스트 포함 총 29개.

기준 커밋: `c409ce16b577351b389c1ba20fcb7bbefe284555`. 로컬 검증 완료 후 사용자의 별도 요청으로 커밋·푸시를 진행합니다. 운영 CMS 전송 검증은 포함하지 않습니다.

## 반영 내용

- 검수 콘솔: 전체 브랜드/브랜드별 상품 페이지 조회, 실패/반복 페이지 보류, 실제 연결 상품 ID·제품 이미지 로드 확인, 첨부 전체 확인.
- 매칭: 포장 용량과 호수/함량 구분, 실제 옵션 조회 재시도, 중복 후보 선택, 단어 누락·콜라보/에디션은 추천 후보로 분리.
- 판정: 짧은 정상 문구 및 중복 본문 수집 오탐 방지, 금지 품목의 허용 키워드 우회 방지. 사진 크기로 도용을 확정하지 않음.
- 화면: 기본 미선택, 선택 복원, CMS 제품 이미지·상품명·본문 표시, 직접 미노출 판단 버튼, 후보 상품 선택 후 템플릿 미리보기.
- 실행: 상세 재조회 비교, 액션 상한, 전송 전 영속 기록, 결과 확인 후 성공 집계, 불명확한 결과 재실행 차단, 같은 출처 탭의 Web Locks 지원, API 요청 시간 제한.
- 기록: 날짜별 스캔 보관, 전체 실행 이력 백업, 업무일지로 출처/창/일회성 식별자 검증 후 전달. 리뷰 ID 중복 제외 및 동일 날짜 감사기록 병합.
- 업무일지: 즉시 로컬 저장, 계정별 분리, 버전별 직렬 업로드, 실패 시 미전송 상태 보존, 조건부 서버 업데이트, 충돌 비교/백업/선택, 잘못된 가져오기 검증.
- 보조 도구: 고정 대상 적용기/검색기 중단, 수집기 중복 실행·불완전 목록 방지, 인증 수집 범위 제한, 정찰기 종료·기록 상한·민감 키 가림.
- 배포 파일: 로컬 단일 HTML을 현재 웹 앱에서 생성하도록 통합. 기존 내장 Chart 번들 유지. 외부 차트 로드 실패 시에도 앱 동작.
- 기존 `data/log.json`, `templates.json`은 변경하지 않았습니다.

## 확인한 것

- `npm test`: 23개 성공. 판정/매칭, 실행 취소, 재실행 차단, 결과 미확정, 저장 실패, 동기화 실패/충돌/실행 중 수정, 데이터 검증 및 구문 검사.
- `npm run build`: 단일 HTML 재생성 성공.
- `git diff --check`, `git fsck --full`, rollback bundle 검증 성공.
- 외부 통신을 차단한 로컬 브라우저: 입력 즉시 저장 → 새로고침 복원, 같은 결과 재전달 → 신규 0건, 검수 기록 탭 표시 확인.
- 사용자가 제공한 이전 승인 로그의 HTTP 201 / `status: APPROVED` 응답 대조.

## 운영 검증이 남은 것

실제 CMS의 전체 페이지/브랜드/옵션 스키마, 수정요청 후 `lastRevisedAt`, Supabase RLS와 두 계정/두 기기 충돌은 모의 테스트만으로 확정할 수 없습니다. 이번 작업에서 운영 전송은 하지 않았습니다. `ROLLBACK.md`의 순서로 읽기 검증 후, 별도 승인 하에 실제 1건부터 검증해야 합니다.

이미지 의미와 발색샷은 사람이 확인합니다. 새 브랜드/상품 등록은 자동 실행하지 않습니다. 검수 상세는 브라우저/JSON 보관이며 Supabase에는 월별 업무일지만 동기화합니다. 브라우저·기기를 넘는 CMS 중복 실행 잠금은 제공하지 않습니다.
